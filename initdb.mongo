conn = new Mongo();
db = conn.getDB("easytext-messenger");
db.dropDatabase();
imgHost = '/';

result = db.chatrooms.insert([
	{
		_id: 'room-1',
		name: 'Everyone in EasyText',
	    image: imgHost+'assets/everyone-icon.png'
	}
]);

print(result);

result = db.conversations.insert([
	{
		room_id: 'room-1',
		timestamp: 1596233125227,
		messages: [{
				username: 'alice',
				text: 'Hello!'
			}, {
				username: 'bob',
				text: 'Hi!'
			}]
	}
]);

print(result);